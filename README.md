# Student-Database-Management-System
The Student Database Management System is a C program used to manage student details. It uses linked lists for memory storage and files for permanent storage. The program supports adding, searching, updating, deleting, sorting, and counting student records, demonstrating dynamic memory and file handling in C.
